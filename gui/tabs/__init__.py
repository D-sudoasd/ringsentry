"""Tab modules — individual pages in the main Notebook widget.

Modules
-------
io_tab
    Tab 1: Input/Output — directory selection, file list, HDF5 path.
processing_tab
    Tab 2: Preprocessing — dark/flat/mask, ROI, intensity controls.
output_tab
    Tab 3: Output Format — format selection, XY options, overwrite.
geometry_tab
    Tab 4: Geometry Transforms — rotation, flip, binning, normalization.
q_calculator_tab
    Tab 5: Q Calculator — Q / 2θ / pixel conversion with detector view.
"""
